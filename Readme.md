# README #

This repository, at local repo's directory level `~/.dotfiles', contains:

    .bashrc
    .bash_aliases
    .bash_profile
    .bash_logout
    .profile
    .inputrc
    .gitconfig
    .tmux.conf
    .vimrc
    .pyrc
    .pylintrc
    .forward
    .wget-hsts
    .xinitrc
    .xbindkeysrc
