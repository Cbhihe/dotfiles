# README #

This repository at directory level `~/.dotfiles' contains:

    .bashrc
    .bash_aliases
    .bash_profile
    .bash_logout
    .inputrc
    .pyrc
    .gitconfig
    .tmux.conf
    .vimrc
    .pylintrc
    .forward
    .wget-hsts
    .xinitrc
    .xbindkeysrc
